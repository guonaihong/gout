package main

func main() {
	/*
		// 3.实现io.Reader接口的任何类型
		fmt.Printf("=====3.=====send string========\n")
		fd, err := os.Open("./rsp_header.go")
		if err != nil {
			fmt.Printf("%s\n", err)
			return
		}
		defer fd.Close()

		err = gout.POST(":8080/req/body").
			Debug(true).
			SetBody(fd).
			Do()

		if err != nil {
			fmt.Printf("%s\n", err)
			return
		}
	*/
}
